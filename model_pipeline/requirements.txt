# torch is installed separately in Dockerfile.pipeline via --index-url
# to get the CPU-only wheel. Do NOT add torch here or pip will pull CUDA (~2.5GB).
transformers==4.41.0
onnx==1.16.0
onnxruntime==1.18.0
optimum[onnxruntime]==1.21.0
protobuf==5.27.0
numpy==1.26.4
google-cloud-storage==2.14.0
google-auth==2.29.0
tqdm==4.66.4